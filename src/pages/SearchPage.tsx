/**
 * Search page — D4 will implement sticker lookup by code
 * with result display and owned toggle.
 */
export function SearchPage() {
  return (
    <div className="text-center py-12">
      <h2 className="font-heading text-2xl font-bold text-ink mb-2">
        🔍 Search Stickers
      </h2>
      <p className="text-ink-muted">
        Search coming in D4 &mdash; exact sticker code lookup (e.g. &ldquo;ARG_01&rdquo;).
      </p>
    </div>
  );
}
